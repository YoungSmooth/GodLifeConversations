import 'package:flutter/material.dart';

class DesktopAllHeader extends StatelessWidget {
  final FileImage imagePath;
  final String headerName;

  const DesktopAllHeader(
      {super.key, required this.imagePath, required this.headerName});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
